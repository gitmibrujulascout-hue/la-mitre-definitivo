{
  "name": "PreEncargoTienda",
  "type": "object",
  "properties": {
    "beneficiario_id": {
      "type": "string",
      "description": "ID del beneficiario que solicita el pre-encargo"
    },
    "beneficiario_nombre": {
      "type": "string",
      "description": "Nombre del beneficiario"
    },
    "producto_id": {
      "type": "string",
      "description": "ID del producto"
    },
    "producto_nombre": {
      "type": "string",
      "description": "Nombre del producto (snapshot)"
    },
    "producto_imagen_url": {
      "type": "string",
      "description": "URL de la imagen del producto (snapshot)"
    },
    "talle": {
      "type": "string",
      "description": "Talle solicitado (si aplica)"
    },
    "cantidad": {
      "type": "number",
      "default": 1,
      "description": "Cantidad solicitada"
    },
    "precio_unitario": {
      "type": "number",
      "description": "Precio unitario al momento del pre-encargo"
    },
    "monto_total": {
      "type": "number",
      "description": "Monto total del pre-encargo"
    },
    "monto_pagado": {
      "type": "number",
      "default": 0,
      "description": "Monto ya pagado (se\u00f1a o pago parcial) por el beneficiario"
    },
    "fecha_pago": {
      "type": "string",
      "format": "date",
      "description": "Fecha del \u00faltimo pago registrado"
    },
    "forma_pago": {
      "type": "string",
      "enum": [
        "Efectivo",
        "Transferencia",
        "Cr\u00e9dito actividad"
      ],
      "description": "Forma de pago de la se\u00f1a/pago"
    },
    "estado": {
      "type": "string",
      "enum": [
        "Pendiente",
        "Confirmado",
        "Entregado",
        "Cancelado"
      ],
      "default": "Pendiente",
      "description": "Estado del pre-encargo"
    },
    "stock_reservado": {
      "type": "boolean",
      "default": false,
      "description": "Si el stock ya fue reservado (decrementado) al crear el pre-encargo. Evita restaurar stock duplicado al cancelar."
    },
    "fecha": {
      "type": "string",
      "format": "date",
      "description": "Fecha del pre-encargo"
    },
    "fecha_confirmacion": {
      "type": "string",
      "format": "date",
      "description": "Fecha en que el admin confirm\u00f3 el pre-encargo"
    },
    "observaciones": {
      "type": "string",
      "description": "Notas del admin"
    }
  },
  "required": [
    "beneficiario_id",
    "producto_id",
    "cantidad",
    "fecha"
  ],
  "rls": {
    "read": {},
    "create": {},
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}