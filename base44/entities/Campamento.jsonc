{
  "name": "Campamento",
  "type": "object",
  "properties": {
    "nombre": {
      "type": "string",
      "description": "Nombre del campamento"
    },
    "fecha_inicio": {
      "type": "string",
      "format": "date",
      "description": "Fecha de inicio"
    },
    "fecha_fin": {
      "type": "string",
      "format": "date",
      "description": "Fecha de fin"
    },
    "costo_por_persona": {
      "type": "number",
      "description": "Costo por persona del campamento (ni\u00f1os)"
    },
    "costo_adultos": {
      "type": "number",
      "description": "Costo por persona adulta (si adultos_pagan es true)"
    },
    "adultos_pagan": {
      "type": "boolean",
      "default": false,
      "description": "Si los adultos/voluntarios deben abonar el campamento"
    },
    "ramas_participantes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Ramas que participan"
    },
    "beneficiarios_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs de beneficiarios (ni\u00f1os) que asisten"
    },
    "adultos_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs de adultos/voluntarios que asisten"
    },
    "ubicacion": {
      "type": "string",
      "description": "Ubicaci\u00f3n del campamento"
    },
    "observaciones": {
      "type": "string",
      "description": "Notas adicionales"
    }
  },
  "required": [
    "nombre",
    "fecha_inicio",
    "costo_por_persona"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}